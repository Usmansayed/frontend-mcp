# Exploration Log — Batch 2 (Forests F07–F12)

**Date:** 2026-07-12
**Author:** research (assisted)
**Scope:** SEO/AI visibility, design review/consistency depth, release/regression, production maintenance, migration/refactor, global failure/recovery.
**States delivered:** 66 leaf states. Combined with Batch 1: **150 states total** (at target minimum).

---

## F07 SEO & AI Visibility (13 states, target 12–18)

### Branch: mode variants (dev vs pro vs auth-required)
- **Kept:** `audit_needed` (landing + marketing), `audit_completed_dev`, `audit_completed_pro`, `pro_mode.needs_auth`.
- **Rationale:** Distinct auth posture drives distinct next actions.

### Branch: AI visibility on vs off vs reviewed
- **Kept:** `ai_readiness_reviewed`, `ai_visibility_disabled`. Explicit opt-out state.
- **Rationale:** A-20260712-001 mandates modeling both.

### Branch: fix loop (applying → re-audit)
- **Kept:** `fix_applying`, `re_audit_after_fix`.

### Branch: archetype-specific
- **Kept:** `ecom.ecommerce_indexability_check` (CSR risk), `blog.content_gap` + `content_gap_verified` (AI-visibility target).
- **Discarded:** per-recommendation-type states (meta title vs H1 vs schema). Recommendation type is a tag on `fix_applying`, not a state.

### Branch: audit diff over time
- **Kept:** `audit_diff_open` (regression variant for SEO evidence).

### Cross-forest edges
- landing.S07.new_feature.landing_hero.verified → landing.S08.seo_campaign.audit_needed (F04→F07)
- landing.S08.seo_campaign.re_audit_after_fix → marketing.S10.release_prep.regression_baseline_capture (F07→F09)

---

## F08 Design Review & Consistency (10 states, target 12–18)

### Branch: multi-page vs single-page audit
- **Kept:** `multi_page_scan` distinctly; single-page variants live in F02.

### Branch: fix disposition variants
- **Kept:** `propose_fix_open`, `exception_documented`. Both are decision states.

### Branch: standards drift and rewrite
- **Kept:** `token_drift_detected`, `standards_updated`. Drift is regressed evidence; updated standards is a fresh baseline.

### Branch: Design Sense vs Consistency
- **Kept:** `marketing.S09.consistency_cleanup.design_sense_findings` separate from `dssite.S09.consistency_cleanup.audit_findings_open`.
- **Rationale:** Different modules, different verification requirements.

### Branch: knowledge query
- **Kept:** `knowledge_query_result`, `knowledge_query_empty`. The empty case funnels to F02's `pdg_empty`.

### Branch: aspirational token import
- **Kept:** `marketing.S09.consistency_cleanup.token_import_pending_scaffold` (`mcp_ready: false`).

### Branch: component library API stability
- **Kept:** `complib.S09.consistency_cleanup.api_stability_check` — bridges F08 to F11.

### Discarded
- Per-rule-family states (spacing vs color vs typography). Rule family is a tag on audit findings.

---

## F09 Release & Regression (10 states, target 10–15)

### Branch: baseline capture + storage
- **Kept:** `regression_baseline_capture`, `baseline_stored`, `baseline_stale`.

### Branch: staging variants
- **Kept:** `staging_verify`, `staging_verified`, `staging_regression`. Verify vs verified are distinct — one is in-progress, the other terminal.

### Branch: smoke test / code review / env misconfig
- **Kept:** `multi_page_smoke_test`, `code_review_pending`, `env_misconfig_pre_release`.

### Branch: production entry
- **Kept:** `marketing.S11.production.live_and_monitored` here rather than F10 because it's the natural exit from release.

### Discarded
- Per-branching-strategy variants (trunk-based vs gitflow). Doesn't change state fingerprint.

---

## F10 Production Maintenance (9 states, target 10–15)

### Branch: hotfix life cycle
- **Kept:** `hotfix_requested`, `hotfix_deployed`, `rollback_required`.

### Branch: monitoring surfaces
- **Kept:** `drift_scheduled_reaudit`, `user_report_received`, `pdg_stale_refresh_scheduled`, `dependency_upgrade_offered`.

### Branch: archetype-specific incidents
- **Kept:** `ecom.checkout_incident` — ecommerce revenue path.

### Branch: non-code work
- **Kept:** `documentation_update`. Confirms modules_must_not_execute for visual_browser during pure doc changes.

### Discarded
- "Weekly re-audit fires ok" (no interesting decision).

---

## F11 Migration & Refactor (10 states, target 12–18)

### Branch: framework migration mode
- **Kept:** `plan_ready`, `incremental_progress`, `big_bang_progress`, `completed`, `rollback`.

### Branch: redesign
- **Kept:** `baseline_before`, `per_page_swap`, `completed`.

### Branch: component library upgrade
- **Kept:** `complib.S12.migration.consumer_notification` — breaking change externalization.

### Branch: legacy maintenance
- **Kept:** `legacy.S12.legacy_maintenance.dependency_upgrade` (M6 maturity).

### Discarded
- Design system re-tokenization as its own forest branch. Covered adequately via `dssite.S09.consistency_cleanup.standards_updated`.

---

## F12 Global Failure & Recovery (12 states, target 8–12)

### Kept
- `dev_server_down`, `auth_gate.requires_human`, `figma_connect_failed`, `upstream_degraded`, `verify_loop_exhausted`, `tooling_broken`, `pdg_refresh_failed`, `session_lost`, `repo_unavailable`, `env_misconfigured`, `user_abandoned`, `verify_success_terminal`.

### Notes
- These states are referenced from every forest as `failure_states` or `possible_next_states`.
- `verify_success_terminal` is the canonical closer for happy paths.
- `auth_gate.requires_human` and `user_abandoned` are the only ones with no automatic recovery.

### Discarded
- Separate global states for each provider (`global.lighthouse_down`, `global.librecrawl_down`). Merged into `global.upstream_degraded` since recovery is identical.
- `global.mcp_bug` — bugs in the MCP itself are out of scope for the state space; they are engineering issues.

---

## Totals and validation

- **Batch 1:** 84 states
- **Batch 2:** 66 states
- **Total:** **150 states** (target 150–250; at minimum by design)
- **`mcp_ready: false` states:** 3 (component live install, design reference registry capture, token import) — all with `mcp_ready_gap` referencing Report 01.
- **`state_confidence: speculative`:** limited to the 3 scaffold states.
- **`global.*` states:** 12 (all in F12).

## Deferred to Phase 4

- Cluster fingerprinting.
- `merged_states.md` (equivalence notes without deletion).
- `parent_cluster` field back-fill on each state YAML.
- `lifecycle_stage_state_map.yaml`.
- `state_graph_index.json` full population.
