## After (Tweak B)

- Design/redesign/system_setup only: when `suggested ≠ gate.next` and claim prohibited → align suggestion to gate.
- Form playbooks / feature_incremental untouched.
- Portfolio: snapshot/figma paid ⇒ inspiration not listed unpaid.

Scenario `one_voice_after_observe.yaml` green: suggested=`design_snapshot` == next (redesign snapshot-first).

## Verdict

**Win for design episodes.** Coordination speaks with one next. Remaining: backlog may still show `design_reference` after snapshot supersede (decision ledger vs evidence terminals) — candidate for EXP-004.

## Next

EXP-003 settings ship (surface judgment). EXP-004 supersede clears unresolved design_reference when snapshot paid.
