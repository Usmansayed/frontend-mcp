# EXP-027 — Adoption: evidence before implementation

## Goal

Prove the host-visible coordinator channel blocks broad greenfield UI before design evidence:

1. `agent_summary.coordinator` + `recommended_next` present after session start
2. Gate prohibits `claim_complete` / `broad_visual_implementation` while design reference unpaid
3. After inspiration/snapshot paid, coordinator still surfaces remaining unpaid — not “implement freely”

## Scenario

`scenarios/greenfield_evidence_before_impl.yaml`

## Win

Adoption spine is measurable: design evidence (or honest skip) before claim-path opens.
