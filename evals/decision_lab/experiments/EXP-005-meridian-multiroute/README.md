## After

Multi-route Meridian path green:
- `surface_type=mixed` from start through settings snap  
- After dash snapshot: backlog ≠ design_reference; suggested == next == component_search_plan  
- Portfolio paid observe+snapshot+component  

## Verdict

**Win.** One episode can own dashboard+settings without re-tunneling on inspiration after measured snapshot.
