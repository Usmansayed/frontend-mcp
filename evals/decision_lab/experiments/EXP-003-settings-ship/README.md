## After

Episode start with settings intent → `surface_type=settings_form` → `build_ship_council` on full-bleed settings fixture:
- `settings_form_measure` present  
- `equal_weight_kpi_cluster` absent  
- `ship_gate.surface_type=settings_form`

pytest `test_exp003_settings_ship_signals_from_episode` green.

## Verdict

**Win.** Coordination owns surface-aware ship judgment; settings are not scored as KPI dashboards.

## Next

EXP-004 — supersede / resolve design_reference when snapshot paid so backlog.top stops tunneling on inspiration after measured evidence exists.
