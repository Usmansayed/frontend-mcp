## After

`coordination_evidence.status=succeeded` correctly succeeds; `selected.id` counts as foundation choice; component leaves backlog.

## Verdict

**Win.** Critical ledger bug fixed — was silently noop-ing tool success.
