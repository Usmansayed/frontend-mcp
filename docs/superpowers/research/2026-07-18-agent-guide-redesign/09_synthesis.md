# 09 — Synthesis (merged research agents)

**Merged from:** [Research MCP agent failures](8d66f0be-54f6-43e4-a717-d3861a6488ef) · [Research situation evidence paths](a66ea78d-aab1-4d98-a5dc-9af725a73dd2)

## One-sentence verdict

Agents skip tools because they treat **singular `gate.next` / `recommended_evidence` / `backlog.top`** as the plan, while MCP already exposes a multi-family scoreboard; clean scenario guides beat more gates (EXP-025/026).

## Confirmed by both research tracks

| Finding | Support |
|---------|---------|
| Tunnel is the #1 failure | EXP-026 2/48 vs 48/48; live EXP-025; code host_action appends unpaid warning |
| Portfolio advisory ≠ agent binding | `episode_portfolio` note vs §2b |
| Snapshot supersedes inspiration | portfolio + redesign workflow |
| Sections/ship ≠ page verify | gate ladder + unpaid families |
| Sticky design survives verify-fail | `episode_design_scope` retry counter |
| Empty unpaid off design scope | `design_scope_applies` / initiative_on |
| Class name mismatch | agent greenfield/polish vs code design_driven/surgical |

## Gate / host_action priority (for Done card)

1. SECTION CHECKLIST  
2. BLOCKED (structural)  
3. RESIDUE SCAN  
4. SHIP GATE  
5. EVIDENCE PLAN  
6. Base influence prose  

Plus: if ≥2 unpaid → “Portfolio unpaid: … (do not tunnel).”

## Influence quick map

| Level | When (code) | Guide posture |
|-------|-------------|---------------|
| structural | early + structural unresolved / design early | Evidence before large code |
| balanced | default design mid / feature | Evidence → act → verify → ladder |
| minimal | hotfix/surgical/debug / hard polish | Observe → fix → verify |
| maintenance | production band | Light; sticky design still owes ladder |

## Design implication (not yet implemented)

**Primary pack (short):** scoreboard loop + hard fails + situation cards (greenfield, redesign/mockup, feature, hotfix/polish, forms).  
**Secondary:** existing `perception://` workflows as deep reference only.  
**MCP:** keep facts simple; no new unpaid claim gates.  
**Promote path:** Guide Lab first → validate → then dual always-on short + methodology cards (open Q1).

## Research status

Notes 01–09 complete. **Q1 = C accepted.** Implementing dual always-on short + `perception://guide/*` cards; Guide Lab arm_b synced; re-test quizzes.
