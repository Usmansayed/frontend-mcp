# `frontend-perception-engine` (PyPI) — legacy name

This is a **thin reverse alias**. All code lives in [`frontend-mcp`](https://pypi.org/project/frontend-mcp/).

```bash
pip install frontend-mcp
```

Installing this package just pulls `frontend-mcp` at the same version.
