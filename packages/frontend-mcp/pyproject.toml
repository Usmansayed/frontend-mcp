# This alias package is retired. See DEPRECATED.md.
# Publishing is disabled — use the root frontend-perception-engine package only.
[project]
name = "frontend-mcp-DEPRECATED-DO-NOT-PUBLISH"
version = "0.0.0"
description = "RETIRED. Install frontend-perception-engine instead."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
