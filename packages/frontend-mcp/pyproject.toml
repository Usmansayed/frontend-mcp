# Do not publish — root package is frontend-mcp now.
[project]
name = "frontend-mcp-DO-NOT-PUBLISH-FROM-HERE"
version = "0.0.0"
description = "RETIRED folder. Root pyproject.toml is frontend-mcp."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
