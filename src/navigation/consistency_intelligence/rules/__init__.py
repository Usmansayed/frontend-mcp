"""Consistency rule validators (planned).

Subpackages to add:
- tokens/ — token extraction and drift detection
- spacing/ — spacing scale adherence
- typography/ — type scale and font consistency
- color/ — palette and semantic color usage
- layout/ — grid, radius, shadow consistency
- components/ — cross-component visual parity
- states/ — hover/focus/active/disabled consistency
"""
